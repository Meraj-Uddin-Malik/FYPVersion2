import 'package:flutter/material.dart';

class StationfinderScreen extends StatelessWidget {
  const StationfinderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Staion FInder"),),
    );
  }
}
